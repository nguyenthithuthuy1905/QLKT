@model List<QLKT.Models.SuCoBatThuong>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "Danh sách sự cố bất thường";
}

<h2>Danh sách sự cố bất thường</h2>

<!-- Form tìm kiếm -->
<form method="get">
    <input type="text" name="search" placeholder="Nhập mã sự cố..." />
    <button type="submit">Tìm kiếm</button>
</form>

<!-- Hiển thị thông báo nếu không tìm thấy -->
@if (!string.IsNullOrEmpty(ViewBag.Message))
{
    <div class="alert alert-warning mt-3">
        @ViewBag.Message
    </div>
}

<table class="table">
    <thead>
        <tr>
            <th>Mã Sự Cố</th>
            <th>Loại Sự Cố</th>
            <th>Ngày Thi</th>
            <th>Hành Động</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.MaSuCo</td>
                <td>@item.LoaiSuCo.TenLoaiSuCo</td>
                <td>@item.NgayThi</td>
                <td>
                    <a href="/SuCo/SuaSuCo?id=@item.MaSuCo" class="btn btn-warning">Sửa</a>
                    <a href="/SuCo/XoaSuCo?id=@item.MaSuCo" class="btn btn-danger">Xóa</a>
                </td>
            </tr>
        }
    </tbody>
</table>
